# Parky

Parky is a real-time parking confidence platform.

## Repo structure
- `apps/web` – Next.js web app (portfolio demo)
- `apps/mobile` – React Native (Expo) mobile app (coming soon)
- `docs` – scope, requirements, architecture, threat model, roadmap

## Run the web app
```bash
cd apps/web
npm install
npm run dev
